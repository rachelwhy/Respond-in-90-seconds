# 核心依赖（版本锁定）
fastapi==0.115.0
uvicorn==0.30.0
openpyxl==3.1.0
python-docx==1.1.0
requests==2.31.0
rapidfuzz==3.9.0
openai==2.31.0  # 与 langextract[openai] 兼容，支持 OpenAI 兼容 API（DeepSeek/Qwen）
python-multipart==0.0.9
python-dotenv==1.0.0
bcrypt==4.0.1
passlib[bcrypt]==1.7.4

# 文本处理（numpy 需与 sentence-transformers / torch / docling 共存；2.2.x 已与 QA 栈联调验证）
numpy>=1.24.0,<3.0.0
jieba==0.42.1

# 文档问答（POST /api/qna/ask）：默认 LangChain ConversationalRetrievalChain + Chroma + HuggingFaceEmbeddings；回退为 BM25 + 向量混合（rank-bm25 + sentence-transformers）
langchain>=0.3.13,<0.4.0
langchain-community>=0.3.13,<0.4.0
langchain-core>=0.3.13,<0.4.0
chromadb>=0.5.0
rank-bm25==0.2.2
sentence-transformers==5.4.1

# 图片处理与OCR
Pillow==10.0.0
opencv-python==4.8.0
pytesseract==0.3.10
pdf2image==1.16.3

# 云OCR SDK（可选，用于混合方案）
# baidu-aip==4.16.6  # 百度OCR
# tencentcloud-sdk-python==3.0.0  # 腾讯云OCR

# 系统依赖说明：
# 1. Tesseract OCR引擎：需要单独安装并添加到PATH
#    Windows: 下载安装包 https://github.com/UB-Mannheim/tesseract/wiki
#    Linux: sudo apt-get install tesseract-ocr tesseract-ocr-chi-sim
#    macOS: brew install tesseract
# 2. Poppler工具：用于PDF转图片
#    Windows: 下载安装包 https://github.com/oschwartz10612/poppler-windows/releases
#    Linux: sudo apt-get install poppler-utils
#    macOS: brew install poppler

# 新增依赖：Docling + LangExtract 重构
docling>=2.0.0
langextract[openai]>=0.1.0  # 包含 OpenAI 兼容 API 支持，用于 DeepSeek/OpenAI/Qwen
diskcache>=5.6.3
prometheus-client>=0.20.0
# 可选：A23_USE_LITELLM=true 时统一走 LiteLLM（多模型/重试/路由）
litellm>=1.40.0

# 数据库（MySQL 入库）
pymysql>=1.1.0

# QnA 回退路径仍依赖 rank-bm25 + sentence-transformers；环境变量 A23_QNA_USE_LANGCHAIN=false 可仅用混合检索。
